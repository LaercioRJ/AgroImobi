import 'package:flutter/material.dart';

class TerrainVisualization extends StatelessWidget {
  const TerrainVisualization({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Visualização de Terrenos'),
      ),
    );
  }
}